<h1>陰キャ一覧</h1>
<table class="table table-hover">
  <thead>
  <tr>
    <th>ユーザー</th>

  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td>
        <%= link_to(user.name,  user_path(id: user.id)) %>
      </td>

    </tr>
  <% end %>
  </tbody>
</table>
